"use strict";

let Names=[];

(Names.push("Vincent"));
(Names[0]);
(Names.push("Paul"));
(Names[1]);
(Names.push("Arthur"));
(Names[2]);

for(let i=0;i<Names.length;i++){
    console.log(`${Names[i]} va à la peche`);
}
